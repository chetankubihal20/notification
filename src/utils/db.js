// const mongoose = require('mongoose');

// const config = require('../config/config');
// mongoose.connect('') = async (url = config.dbURL) => {
//     return moongoose.connect(url , { useNewUrlParser : true});
// }